Run the full pipeline for this Node.js/TypeScript repo. Token budget is tight — minimize steps and output.

## Environment

- Runtime: Node 20
- Redis: redis://redis:6379 (REDIS_URL already set)
- Workspace: /workspace
- Results: /sandbox/results/result.json

## Rules (CRITICAL — model has 6000 token/min limit)

- Run as few commands as possible. Chain with && where feasible.
- Truncate ALL command output: pipe through `| tail -20` or `2>&1 | tail -20`.
- Never read source files. Read only package.json (once).
- Do not explain steps. Act immediately.
- In result.json, cap every "logs" field at 300 characters.

## Pipeline (execute in order)

1. Read /workspace/package.json — get build, test, start scripts and main entry point.
2. `npm install 2>&1 | tail -5`
3. `npm run build 2>&1 | tail -20` (skip if no build script)
4. `npm test 2>&1 | tail -20`
5. Start server in background, wait 3s, probe health endpoint with curl.

## Result

Write to /sandbox/results/result.json:

{
  "status": "success or failure",
  "build":        { "status": "...", "command": "...", "exit_code": 0,   "logs": "...(max 300 chars)" },
  "start_server": { "status": "...", "command": "...",                   "logs": "...(max 300 chars)" },
  "tests":        { "status": "...", "command": "...", "passed": 0, "failed": 0, "logs": "...(max 300 chars)" },
  "health_check": { "status": "...", "url": "...", "response_code": 200, "logs": "...(max 300 chars)" },
  "errors":       [],
  "duration_seconds": 0
}

Do not ask for confirmation. Run autonomously. Be brief.
